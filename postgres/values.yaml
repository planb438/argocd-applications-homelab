auth:
  postgresPassword: "MyStr0ngP@ssw0rd!"
  username: "myapp_user"
  password: "UserP@ssw0rd123!"
  database: "myapp_db"
  existingSecret: ""

primary:
  persistence:
    enabled: true
    size: 10Gi
    storageClass: "standard"
  resources:
    requests:
      memory: "512Mi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "500m"

service:
  type: ClusterIP
  port: 5432

initdbScripts:
  init.sql: |
    CREATE EXTENSION IF NOT EXISTS pgcrypto;
